diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -171,6 +171,7 @@ endif()
 option(BCM_VC "Enable Broadcom Videocore 3 support" OFF)
 set(WITH_BCM_VC FALSE)
 set(VC4_CMA FALSE)
+set(WITH_VULKAN FALSE)
 
 # error out on depricated PLATFORM values
 if(PLATFORM STREQUAL "rpiv1" OR PLATFORM STREQUAL "armv7-generic" OR PLATFORM STREQUAL "armv8-generic")
@@ -204,6 +205,54 @@ elseif(PLATFORM STREQUAL "v4cma")
 	add_compile_options(-march=armv8-a)
 	add_compile_definitions(HAVE_VC4_CMA)
 	list(APPEND rtl_airband_extra_sources samplefft_v4cma.cpp)
+elseif(PLATFORM STREQUAL "vulkan")
+	set(WITH_VULKAN TRUE)
+
+	find_path(VULKAN_INCLUDE_DIR vulkan/vulkan.h)
+	find_library(VULKAN_LIBRARY NAMES vulkan)
+	if(NOT VULKAN_INCLUDE_DIR OR NOT VULKAN_LIBRARY)
+		message(FATAL_ERROR "PLATFORM=vulkan requires Vulkan headers and loader library")
+	endif()
+
+	find_program(VULKAN_GLSLC glslc)
+	find_program(VULKAN_GLSLANG_VALIDATOR glslangValidator)
+	if(NOT VULKAN_GLSLC AND NOT VULKAN_GLSLANG_VALIDATOR)
+		message(FATAL_ERROR "PLATFORM=vulkan requires glslc or glslangValidator to compile the FFT compute shader")
+	endif()
+
+	set(VULKAN_SHADER_SOURCE "${CMAKE_CURRENT_SOURCE_DIR}/vulkan_fft.comp")
+	set(VULKAN_SHADER_SPV "${CMAKE_CURRENT_BINARY_DIR}/vulkan_fft.spv")
+	set(VULKAN_SHADER_HEADER "${CMAKE_CURRENT_BINARY_DIR}/vulkan_fft_spv.h")
+	if(VULKAN_GLSLC)
+		add_custom_command(
+			OUTPUT ${VULKAN_SHADER_SPV}
+			COMMAND ${VULKAN_GLSLC} -O --target-env=vulkan1.0 -fshader-stage=compute
+				-o ${VULKAN_SHADER_SPV} ${VULKAN_SHADER_SOURCE}
+			DEPENDS ${VULKAN_SHADER_SOURCE}
+			VERBATIM
+		)
+	else()
+		add_custom_command(
+			OUTPUT ${VULKAN_SHADER_SPV}
+			COMMAND ${VULKAN_GLSLANG_VALIDATOR} -V -S comp
+				-o ${VULKAN_SHADER_SPV} ${VULKAN_SHADER_SOURCE}
+			DEPENDS ${VULKAN_SHADER_SOURCE}
+			VERBATIM
+		)
+	endif()
+	add_custom_command(
+		OUTPUT ${VULKAN_SHADER_HEADER}
+		COMMAND ${CMAKE_COMMAND} -DINPUT=${VULKAN_SHADER_SPV} -DOUTPUT=${VULKAN_SHADER_HEADER}
+			-P ${CMAKE_CURRENT_SOURCE_DIR}/CMakeModules/embed_binary.cmake
+		DEPENDS ${VULKAN_SHADER_SPV} ${CMAKE_CURRENT_SOURCE_DIR}/CMakeModules/embed_binary.cmake
+		VERBATIM
+	)
+	set_source_files_properties(${VULKAN_SHADER_HEADER} PROPERTIES GENERATED TRUE)
+	list(APPEND rtl_airband_extra_sources vulkan_fft.cpp ${VULKAN_SHADER_HEADER})
+	list(APPEND rtl_airband_extra_libs ${VULKAN_LIBRARY})
+	list(APPEND rtl_airband_include_dirs ${VULKAN_INCLUDE_DIR})
 # native - let the complier optimize to run on local hardware (default)
 elseif(PLATFORM STREQUAL "native")
 	CHECK_CXX_COMPILER_FLAG(-march=native CXX_HAS_MARCH_NATIVE)
@@ -218,7 +267,7 @@ elseif(PLATFORM STREQUAL "generic")
 	# NO-OP
 # error out on unrecongnnized PLATFORM value
 else()
-	message(FATAL_ERROR "Unknown platform '${PLATFORM}'. Valid options are: rpiv2, v4cma, native, and generic")
+	message(FATAL_ERROR "Unknown platform '${PLATFORM}'. Valid options are: rpiv2, v4cma, vulkan, native, and generic")
 endif()
@@ -250,6 +299,7 @@ message(STATUS "- Other options:")
 message(STATUS "  - Platform:\t\t${PLATFORM}")
 message(STATUS "  - Build Unit Tests:\t${BUILD_UNITTESTS}")
 message(STATUS "  - Broadcom VideoCore GPU:\t${WITH_BCM_VC}")
+message(STATUS "  - Vulkan Compute FFT:\t${WITH_VULKAN}")
 message(STATUS "  - VideoCore IV CMA backend:\t${VC4_CMA}")
 message(STATUS "  - NFM support:\t\t${NFM}")
 message(STATUS "  - PulseAudio:\t\trequested: ${PULSEAUDIO}, enabled: ${WITH_PULSEAUDIO}")
diff --git a/src/config.h.in b/src/config.h.in
--- a/src/config.h.in
+++ b/src/config.h.in
@@ -27,6 +27,7 @@
 #cmakedefine WITH_PULSEAUDIO
 #cmakedefine NFM
 #cmakedefine WITH_BCM_VC
+#cmakedefine WITH_VULKAN
 #cmakedefine LIBSHOUT_HAS_TLS
 #cmakedefine LIBSHOUT_HAS_CONTENT_FORMAT
 #define SINCOSF @SINCOSF@
diff --git a/src/rtl_airband.h b/src/rtl_airband.h
--- a/src/rtl_airband.h
+++ b/src/rtl_airband.h
@@ -36,6 +36,8 @@
 #else
 #include "hello_fft/gpu_fft.h"
 #endif
+#elif defined WITH_VULKAN
+#include "vulkan_fft.h"
 #else
 #include <fftw3.h>
 #endif /* WITH_BCM_VC */
@@ -78,6 +80,8 @@ extern "C" void samplefft(sample_fft_arg* a, unsigned char* buffer, float* window,
 
 #define FFT_BATCH 250
+#elif defined WITH_VULKAN
+#define FFT_BATCH 32
 #else
 #define FFT_BATCH 1
 #endif /* WITH_BCM_VC */
@@ -269,11 +273,11 @@ struct demod_params_t {
     int device_start;
     int device_end;
 
-#ifndef WITH_BCM_VC
+#if !defined WITH_BCM_VC && !defined WITH_VULKAN
     fftwf_plan fft;
     fftwf_complex* fftin;
     fftwf_complex* fftout;
-#endif /* WITH_BCM_VC */
+#endif
 };
 
 struct output_params_t {
diff --git a/src/rtl_airband.cpp b/src/rtl_airband.cpp
--- a/src/rtl_airband.cpp
+++ b/src/rtl_airband.cpp
@@ -180,6 +180,10 @@ class AFC {
     float square(const GPU_FFT_COMPLEX* fft_results, size_t index) {
         return fft_results[index].re * fft_results[index].re + fft_results[index].im * fft_results[index].im;
     }
+#elif defined WITH_VULKAN
+    float square(const VulkanComplex* fft_results, size_t index) {
+        return fft_results[index].re * fft_results[index].re + fft_results[index].im * fft_results[index].im;
+    }
 #else
     float square(const fftwf_complex* fft_results, size_t index) {
         return fft_results[index][0] * fft_results[index][0] + fft_results[index][1] * fft_results[index][1];
@@ -265,11 +269,11 @@ void init_demod(demod_params_t* params, Signal* signal, int device_start, int dev
     params->device_start = device_start;
     params->device_end = device_end;
 
-#ifndef WITH_BCM_VC
+#if !defined WITH_BCM_VC && !defined WITH_VULKAN
     params->fftin = fftwf_alloc_complex(fft_size);
     params->fftout = fftwf_alloc_complex(fft_size);
     params->fft = fftwf_plan_dft_1d(fft_size, params->fftin, params->fftout, FFTW_FORWARD, FFTW_MEASURE);
-#endif /* WITH_BCM_VC */
+#endif
 }
@@ -341,6 +345,19 @@ void* demodulate(void* params) {
             break;
 #endif
     }
+#elif defined WITH_VULKAN
+    VulkanFFT fft(fft_size, FFT_BATCH);
+    if (!fft.initialize()) {
+        log(LOG_CRIT, "Unable to initialize Vulkan FFT or FFTW fallback: %s\n", fft.last_error().c_str());
+        error();
+    }
+    if (fft.using_vulkan()) {
+        log(LOG_INFO, "Vulkan FFT: device=%s, workgroup=%zu, batch=%d\n", fft.device_name().c_str(), fft.workgroup_size(), FFT_BATCH);
+    } else {
+        log(LOG_WARNING, "Vulkan FFT unavailable (%s); using %s, batch=%d\n", fft.fallback_reason().c_str(), fft.device_name().c_str(), FFT_BATCH);
+    }
+    VulkanComplex* fftin = fft.input();
+    const VulkanComplex* fftout = fft.output();
 #else
     fftwf_complex* fftin = demod_params->fftin;
     fftwf_complex* fftout = demod_params->fftout;
@@ -362,6 +379,8 @@ void* demodulate(void* params) {
 #ifdef WITH_BCM_VC
     // TODO: change this to std::vector<float> ?
     float ALIGNED32 window[fft_size * 2];
+#elif defined WITH_VULKAN
+    std::vector<float> window(fft_size);
 #else
     std::unique_ptr<float[], decltype(&fftwf_free)> window(fftwf_alloc_real(fft_size), fftwf_free);
 #endif /* WITH_BCM_VC */
@@ -437,6 +456,15 @@ void* demodulate(void* params) {
                     ptr[i].re = scale * (float)buf2[0] * window[i * 2];
                     ptr[i].im = scale * (float)buf2[1] * window[i * 2];
                 }
             }
+#elif defined WITH_VULKAN
+            VulkanComplex* ptr = fftin;
+            for (size_t b = 0; b < FFT_BATCH; b++, ptr += fft_size) {
+                short* buf2 = (short*)(dev->input->buffer + dev->input->bufs + b * bps);
+                for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
+                    ptr[i].re = scale * (float)buf2[0] * window[i];
+                    ptr[i].im = scale * (float)buf2[1] * window[i];
+                }
+            }
 #else
             short* buf2 = (short*)(dev->input->buffer + dev->input->bufs);
             for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
@@ -453,6 +481,15 @@ void* demodulate(void* params) {
                     ptr[i].re = scale * buf2[0] * window[i * 2];
                     ptr[i].im = scale * buf2[1] * window[i * 2];
                 }
             }
+#elif defined WITH_VULKAN
+            VulkanComplex* ptr = fftin;
+            for (size_t b = 0; b < FFT_BATCH; b++, ptr += fft_size) {
+                float* buf2 = (float*)(dev->input->buffer + dev->input->bufs + b * bps);
+                for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
+                    ptr[i].re = scale * buf2[0] * window[i];
+                    ptr[i].im = scale * buf2[1] * window[i];
+                }
+            }
 #else  // WITH_BCM_VC
             float* buf2 = (float*)(dev->input->buffer + dev->input->bufs);
             for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
@@ -472,6 +509,15 @@ void* demodulate(void* params) {
                 samplefft(&sfa, dev->input->buffer + dev->input->bufs + i * bps, window, levels_ptr);
                 sfa.dest += fft->step;
             }
+#elif defined WITH_VULKAN
+            for (size_t b = 0; b < FFT_BATCH; b++) {
+                unsigned char* buf2 = dev->input->buffer + dev->input->bufs + b * bps;
+                VulkanComplex* ptr = fftin + b * fft_size;
+                for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
+                    ptr[i].re = levels_ptr[buf2[0]] * window[i];
+                    ptr[i].im = levels_ptr[buf2[1]] * window[i];
+                }
+            }
 #else
             unsigned char* buf2 = dev->input->buffer + dev->input->bufs;
             for (size_t i = 0; i < fft_size; i++, buf2 += 2) {
@@ -493,6 +539,12 @@ void* demodulate(void* params) {
 #else
         gpu_fft_execute(fft);
 #endif
+#elif defined WITH_VULKAN
+        if (!fft.execute()) {
+            log(LOG_CRIT, "Vulkan FFT execution failed: %s\n", fft.last_error().c_str());
+            do_exit = 1;
+            continue;
+        }
 #else
         fftwf_execute(demod_params->fft);
 #endif /* WITH_BCM_VC */
@@ -516,6 +568,25 @@ void* demodulate(void* params) {
                 }
             }
         }
+#elif defined WITH_VULKAN
+        for (int i = 0; i < dev->channel_count; i++) {
+            float* wavein = dev->channels[i].wavein + dev->waveend;
+            __builtin_prefetch(wavein, 1);
+            const int bin = dev->bins[i];
+            const VulkanComplex* result = fftout + bin;
+            for (int j = 0; j < FFT_BATCH; j++, ++wavein, result += fft_size)
+                *wavein = sqrtf(result->im * result->im + result->re * result->re);
+        }
+        for (int j = 0; j < dev->channel_count; j++) {
+            if (dev->channels[j].needs_raw_iq) {
+                const VulkanComplex* result = fftout;
+                for (int job = 0; job < FFT_BATCH; job++) {
+                    dev->channels[j].iq_in[2 * (dev->waveend + job)] = result[dev->bins[j]].re;
+                    dev->channels[j].iq_in[2 * (dev->waveend + job) + 1] = result[dev->bins[j]].im;
+                    result += fft_size;
+                }
+            }
+        }
 #else
         for (int j = 0; j < dev->channel_count; j++) {
             dev->channels[j].wavein[dev->waveend] = sqrtf(fftout[dev->bins[j]][0] * fftout[dev->bins[j]][0] + fftout[dev->bins[j]][1] * fftout[dev->bins[j]][1]);
@@ -649,6 +720,8 @@ void* demodulate(void* params) {
 
 #ifdef WITH_BCM_VC
                 afc.finalize(dev, i, fft->out);
+#elif defined WITH_VULKAN
+                afc.finalize(dev, i, fftout);
 #else
                 afc.finalize(dev, i, demod_params->fftout);
 #endif /* WITH_BCM_VC */
